= 扩展性与运行时间分析

本章回答两个实际工程问题：一是不启用 Nsight Systems 时，完整跑完五个基准配置需要多长时间；二是在 30 分钟总时间预算内，能否进一步加大问题规模。

== 无 nsys 的完整五配置运行时间

为了测量纯 HPCG 执行时间，编写了 `scripts/benchmark_no_nsys.sh`。该脚本跳过 nsys 采集，仅运行每个配置的基准 HPCG（`rt=60`），并记录每个配置的 wall-clock 时间。五次独立运行的结果如下：

#table(
  columns: (2.5cm, 2.5cm, 2cm, 2.5cm, 2.5cm),
  align: center,
  table.header([配置], [Wall time (s)], [结果], [GFLOP/s], [Exec time (s)]),
  [RTX4090], [138.80], [VALID], [189.07], [134.96],
  [RTX5080], [140.38], [VALID], [115.57], [134.25],
  [DUAL_EQUAL], [143.78], [VALID], [195.17], [137.43],
  [DUAL_EQUAL_128], [156.86], [VALID], [132.20], [151.21],
  [DUAL_HET], [144.67], [VALID], [176.60], [137.66],
)

五次基准配置总计 wall-clock 时间约为 *724.5 秒（约 12.1 分钟）*。因此，在不启用 nsys 的情况下，完整跑完五种配置可以控制在 15 分钟以内，远低于 30 分钟预算。

== 更大规模的可行性实验

在剩余时间内，进一步测试了三种更大规模的双卡配置，均不启用 nsys：

#table(
  columns: (3cm, 2.5cm, 2cm, 2.5cm, 2.5cm, 4cm),
  align: center,
  table.header([配置], [Wall time (s)], [结果], [GFLOP/s], [Exec time (s)], [说明]),
  [DUAL_EQUAL_160], [105.51], [VALID], [200.69], [96.07], [每 rank 160³，全局 160×160×320],
  [DUAL_EQUAL_192], [87.96], [VALID], [198.55], [73.70], [每 rank 192³，全局 192×192×384],
  [DUAL_HET_192], [96.86], [VALID], [201.60], [84.94], [快卡 192³，慢卡 192×192×128],
)

三种更大规模配置均成功完成，且 wall-clock 时间反而比基准的 `rt=60` 更短。原因在于 HPCG 根据问题规模动态调整 CG set 数量：规模越大，每个 set 的工作量增加，达到相同 `rt=60` 所需的 set 数减少，总 wall-clock 时间反而下降。GPU 显存占用在 192³ 时约为 6.5 GB/rank，仍远低于 RTX 5080 的 16 GB 限制。

基准五配置 + 扩展实验总计约 *1014.8 秒（约 16.9 分钟）*，仍远小于 30 分钟限制。因此，在 30 分钟预算内完全可以运行更大规模（如每 rank 192³ 甚至 256³）的双卡 HPCG 实验。

== 扩展性结论

- 无 nsys 的常规五配置跑测约 12 分钟，nsys GPU metrics 采集是主要时间开销。
- 规模增加到 192³ 后，双卡等分和异构配置均能获得约 200 GFLOP/s 的吞吐，且运行时间仍在 90–110 秒之间。
- 异构划分在更大规模下仍保持有效：`DUAL_HET_192` 的 DDOT Max/Min 为 12.18 s / 5.87 s，负载均衡明显优于 `DUAL_EQUAL_192`（14.38 s / 0.03 s）。
